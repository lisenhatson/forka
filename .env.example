# Django Settings
DEBUG=False
ALLOWED_HOSTS=127.0.0.1,localhost,django_backend,backend

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost,http://127.0.0.1

# Database Settings
DB_ENGINE=django.db.backends.postgresql
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=5432

# Email Settings
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_VERIFICATION_REQUIRED=True
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=

SECRET_KEY=
